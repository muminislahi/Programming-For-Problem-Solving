#include <stdio.h>
int main(){
    float a,b;
    printf("\nInput the dividend\n");
    scanf("%f",&a);
    printf("\nInput the divisor\n");
    scanf("%f",&b);
    printf("\nThe quotient when the two numbers are divided is %f",a/b);

  return 0;
}
